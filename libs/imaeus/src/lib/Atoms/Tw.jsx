import React, { createElement, forwardRef } from "react";

// export const Tw = ({ as, children, ...props }) => {
//     const twProps = usePackProps(props, ["display", "flexSize"]);
//     const cns = useTwClassnames(twProps)
//     return createElement(as, props, children);;
// }


// export const TwWithRef = forwardRef(
//     ({ as, children, ...props }) => {
//         const twProps = usePackProps(props, ["display", "flexSize"]);
//         const cns = useTwClassnames(twProps)
//         return createElement(as, props, children);;
//     }
// );

// export const TwMemoised = React.memo( Tw )

// export const TwWithRefMemoised = React.memo( TwWithRef )
